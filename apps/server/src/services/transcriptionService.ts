import { transcribeAudio, transcribeAudioSegments } from '../../lib/transcribeAudio.js';

const transcriptionService = {
  transcribeAudio,
  transcribeAudioSegments,
};

export default transcriptionService;
